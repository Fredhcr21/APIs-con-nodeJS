version: '3.3'

services:
  postgres:
    image: postgres:13
    environment:
      - POSTGRES_DB=APIs-Rest
      - POSTGRES_USER=Jhon
      - POSTGRES_PASSWORD=152124
    ports:
     - 1521:1521
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

  registry:
      image: ibmcom/registry
      environment:
        - PGADMIN_DEFAULT_EMAIL=regis@mail.com
        - PGADMIN_DEFAULT_PASSWORD=1521
      ports:
        - 2222:50
